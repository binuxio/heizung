import React from 'react'
import { View } from 'react-native'


/**
 * The device has this State
 * #### **Schedule Enabled**
 * - ON (By Plan)
 * - ON (Manually but will turn off)
 * - OFF (By Plan)
 * - OFF (Manually but Will turn on) 
 * #### **Schedule Disabled**
 * - ON (Manually)
 * - OFF (Forced)
 **/
const CurrentState = () => {
    return (
        <View>
        </View>
    )
}

export default CurrentState