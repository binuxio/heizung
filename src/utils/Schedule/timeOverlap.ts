import { EventMoment } from "@/types/schedule.types";

export default function (prevEventEndMoment: EventMoment, currentEvent: EventMoment): boolean {
    prevEvent.endMoment.isSameOrAfter(startMoment)
}